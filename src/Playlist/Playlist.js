import React from 'react';

const renderSongs = (array) => {
	array.map((song, index) => (
		<li></li>
	))
}

export default () => (
	<div className='playlist-div'>
		<h2>Your Top 40</h2>
  </div>
);
